/* config.h. Generated by CMake for dart. */
#ifndef DART_CONFIG_H_
#define DART_CONFIG_H_

/* Version number */
#define DART_MAJOR_VERSION 5
#define DART_MINOR_VERSION 1
#define DART_PATCH_VERSION 2

#define DART_VERSION "5.1.2"
#define DART_DESCRIPTION "Dynamic Animation and Robotics Toolkit."

#define DART_VERSION_AT_LEAST(x,y,z) \
  (DART_MAJOR_VERSION > x || (DART_MAJOR_VERSION >= x && \
  (DART_MINOR_VERSION > y || (DART_MINOR_VERSION >= y && \
  DART_PATCH_VERSION >= z))))

#define DART_MAJOR_MINOR_VERSION_AT_LEAST(x,y) \
  (DART_MAJOR_VERSION > x || (DART_MAJOR_VERSION >= x && \
  (DART_MINOR_VERSION > y || (DART_MINOR_VERSION >= y))))

#define DART_VERSION_AT_MOST(x,y,z) \
  (DART_MAJOR_VERSION < x || (DART_MAJOR_VERSION <= x && \
  (DART_MINOR_VERSION < y || (DART_MINOR_VERSION <= y && \
  DART_PATCH_VERSION <= z))))

#define DART_MAJOR_MINOR_VERSION_AT_MOST(x,y) \
  (DART_MAJOR_VERSION < x || (DART_MAJOR_VERSION <= x && \
  (DART_MINOR_VERSION < y || (DART_MINOR_VERSION <= y))))

// Detect the compiler
#if defined(__clang__)
  #define DART_COMPILER_CLANG
#elif defined(__GNUC__) || defined(__GNUG__)
  #define DART_COMPILER_GCC
#elif defined(_MSC_VER)
  #define DART_COMPILER_MSVC
#endif

/* #undef BUILD_TYPE_DEBUG */
#define BUILD_TYPE_RELEASE 1
/* #undef BUILD_TYPE_RELWITHDEBINFO */
/* #undef BUILD_TYPE_MINSIZEREL */

/* #undef HAVE_NLOPT */
/* #undef HAVE_IPOPT */
/* #undef HAVE_SNOPT */
/* #undef HAVE_BULLET_COLLISION */

#define DART_ROOT_PATH "/home/jay/libraries/dart/"
#define DART_DATA_PATH "/home/jay/libraries/dart/data/"

// See #451
#define ASSIMP_AISCENE_CTOR_DTOR_DEFINED 1
#define ASSIMP_AIMATERIAL_CTOR_DTOR_DEFINED 1

#endif // #ifndef DART_CONFIG_H_
